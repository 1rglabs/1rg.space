const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			colors: {
				darkseagreen: "#6CD881",
				mediumorchid: "#7A31FF"
			}
		}
	},

	plugins: []
};

module.exports = config;